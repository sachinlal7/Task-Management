import 'package:flutter/material.dart';

class PRPost extends StatelessWidget {
  const PRPost({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(
      child: Text("PRPost"),
    ));
  }
}
